# The ios/ and android/ folders are generated by prebuild and are gitignored, so
# they exist on this machine but never on the EAS Build worker. Hashing them would
# make the local fingerprint disagree with the build's. The inputs that actually
# determine the generated native project (app.json, plugins/, modules/, native
# dependencies in package.json) are still hashed.
ios/**/*
android/**/*
